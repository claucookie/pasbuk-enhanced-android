package labs.claucookie.pasbuk.domain.model

/**
 * Type of pass based on Apple Wallet pass types.
 */
enum class PassType {
    BOARDING_PASS,
    EVENT_TICKET,
    COUPON,
    STORE_CARD,
    GENERIC
}
